<?xml version="1.0"?>
<project name="testBuild" default="test" basedir="." >
	<property file="project.properties"/>

        <property name="workspace.dir" location="${relative.path}"/>

	
	<target name="test" depends="">
		<echo>Test Call: ${CI_BUILD}</echo>

 

                <ant antfile="${CI_BUILD}"/>
	</target>
	
</project>

