<?xml version="1.0"?>
<project name="testBuild" default="test" basedir="." >
        <property name="workspace.dir" location="."/>
        
        <!-- project.properties -->
	<property file="project.properties"/>

	
	<target name="test" depends="">
		<echo>Test Call: ${SAG_CI_HOME}</echo>

                <ant dir="${SAG_CI_HOME}" antfile="${SAG_CI_HOME}/build.xml" inheritAll="true"/>
	</target>
	
</project>

